# html

Static HTML/CSS/JS prototypes. Owned by `design-agent`.

Prototypes live here first; once a layout is settled it moves into
`website/app/page/` as a PHP view and its styles land in `website/assets/`.

This folder is published on its own as the `html` branch, contents flattened
to the repo root, so a prototype can be served or previewed standalone.
